import { } from "react-redux";
import { Provider } from "react-redux"
import { } from "./store";

import "./App.css";

function App() {

  return (
    <Provider store={store}>
    <>
     
    </>
    </Provider>
  );
}

export default App;
